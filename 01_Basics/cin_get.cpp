#include <iostream>
using namespace std;

int main() {

    int a, b; 
    // cin >> a >> b;  
    a = cin.get(); 
    // cout << "Value of a and b is : " << a <<" "<< b <<endl;
    cout <<"Value of a is : " << a << endl;

    /* so, basically cin.get() gives the ascii value of any character be it tab, enter or space even */

    return 0; 
    
}